import React from 'react';
import PortfolioItem from './PortfolioItem';

const Gallery = props => {
  return (
    <div>
      <PortfolioItem />
      <PortfolioItem />
      <PortfolioItem />
    </div>
  );
};

export default Gallery;
